# Domain document rules

The repository uses a single domain context.

1. Read `CONTEXT.md` before naming modules or changing product behaviour.
2. Read ADRs under `docs/adr/` before changing architecture or persistence choices.
3. Update `CONTEXT.md` when a domain term becomes clearer.
4. Record durable, non-obvious architectural decisions as numbered ADRs.
