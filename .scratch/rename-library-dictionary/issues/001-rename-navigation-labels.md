# Rename admin navigation labels

Status: completed
Labels: ready-for-human

## Acceptance criteria

- `/admin/media` is labeled “Thư viện”.
- `/admin/content-variables` is labeled “Từ điển”.
- Existing Vietnamese Content Registry rows are migrated from prior label variants.
- Targeted navigation regression coverage is updated and passes.
