/** @type {import('prettier').Config} */
const config = {
  plugins: ["prettier-plugin-tailwindcss"],
  printWidth: 110,
  semi: true,
  singleQuote: false,
  trailingComma: "all",
};

export default config;
